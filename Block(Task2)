Input:
12


Output:
+--+
|12|
+--+

-------------------------------------------------------------------------------------------------------------------------

class Block:
    def __init__(self,value):
        self.value = value

if __name__ == "__main__":
    block_value = int(input())
    block_one = Block(block_value)
    x= len(str(block_one.value))
    print('+' + x*'-' + '+')
    print('|{}|'.format(block_one.value))
    print('+' + x*'-' + '+')
